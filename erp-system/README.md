# نظام ERP - بروتوتايب

نظام إدارة موارد المؤسسة (ERP) مبني بـ Node.js و React

## المميزات
- 📊 لوحة تحكم بإحصائيات شاملة
- 📦 إدارة المنتجات والمخزون
- 👥 إدارة العملاء
- 🛒 إدارة المبيعات والطلبات
- 📈 تقارير وتحليلات

## التشغيل

### 1. تثبيت الـ Dependencies
```bash
cd erp-system
npm install
cd client && npm install
```

### 2. تشغيل السيرفر
```bash
npm run server
```
السيرفر هيشتغل على: http://localhost:5000

### 3. تشغيل الـ Client
```bash
cd client
npm start
```
الواجهة هتفتح على: http://localhost:3000

## هيكل المشروع
```
erp-system/
├── server/
│   ├── index.js          # نقطة البداية
│   ├── database/
│   │   ├── db.js         # اتصال قاعدة البيانات
│   │   └── schema.sql    # هيكل الجداول
│   └── routes/
│       ├── customers.js  # API العملاء
│       ├── products.js   # API المنتجات
│       ├── sales.js      # API المبيعات
│       └── reports.js    # API التقارير
├── client/
│   └── src/
│       ├── App.js
│       └── pages/
│           ├── Dashboard.js
│           ├── Products.js
│           ├── Customers.js
│           └── Sales.js
└── package.json
```

## API Endpoints

### العملاء
- `GET /api/customers` - جلب كل العملاء
- `POST /api/customers` - إضافة عميل
- `PUT /api/customers/:id` - تعديل عميل
- `DELETE /api/customers/:id` - حذف عميل

### المنتجات
- `GET /api/products` - جلب كل المنتجات
- `GET /api/products/low-stock` - المنتجات قليلة المخزون
- `POST /api/products` - إضافة منتج
- `PUT /api/products/:id` - تعديل منتج
- `DELETE /api/products/:id` - حذف منتج

### المبيعات
- `GET /api/sales` - جلب كل الطلبات
- `POST /api/sales` - إنشاء طلب جديد
- `PATCH /api/sales/:id/status` - تحديث حالة الطلب

### التقارير
- `GET /api/reports/dashboard` - إحصائيات لوحة التحكم
- `GET /api/reports/top-products` - أكثر المنتجات مبيعاً
- `GET /api/reports/inventory-value` - قيمة المخزون
